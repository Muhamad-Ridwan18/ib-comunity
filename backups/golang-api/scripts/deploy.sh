#!/usr/bin/env bash
# LEGACY: Go Fiber deploy script.
# Active API is Laravel under /backend — this script is kept only for archive/rollback.
echo "This deploy script targets the archived Go API."
echo "Use Laravel deploy docs once cutover is ready."
echo "Archive path: backups/golang-api/"
exit 1
